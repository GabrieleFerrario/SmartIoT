Gabriele Ferrario 817518

Elenco file:
- app.py contiene la web app (folder templates contiene html e css delle varie pagine web)
- Machine_Learning.ipynb: contiene il notebook utilizzato per costruire il modello di Machine Learning che viene utilizzato nel nodemcu
- model.h: file contente la conversione del modello SVM in codice C per poterlo usare nel NodeMCU (effettuata tramite MicroML)
- requirements.txt: contiene tutte le dipendenze (eccetto jupyter notebook che può essere installato tramite il comando pip install notebook) 
da utilizzare per eseguire le varie parti del progetto realizzare in Python
- telegram_bot.py: contiene il codice che implementa il bot Telegram
